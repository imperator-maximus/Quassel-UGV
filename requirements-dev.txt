# Development/test dependencies that are safe to install on Windows.
#
# Use this before pre-deploy test runs from the repository root:
#   python -m pip install -r requirements-dev.txt
PyYAML>=6.0
shapely>=2.0
Flask>=2.3.0
Flask-SocketIO>=5.3.0
